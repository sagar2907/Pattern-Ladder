# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Optional. Without it the system uses the deterministic rule-based query
# parser in src/pattern_ladder/understand/fallback.py, and says so in the UI.
# Everything else -- retrieval, reranking, families, ladders -- runs offline.
#
# Free tier, no card required: https://console.groq.com
GROQ_API_KEY=
